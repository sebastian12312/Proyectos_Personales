use ProyectoVentas
create table usuario(
codigo_usuario varchar(25) primary key,
nickname_usuario varchar(50),
nombre_usuario varchar(50),
apellido_usuario varchar(50),
telefono_usuario varchar(15),
correo_usuario varchar(50),
contraseña_usuario varchar(50),
genero_usuario varchar(15),
fecha_nacimiento date
)
create table datosUsuario(
codigo_usuario varchar(25),
saldo_usuario money,
tipo_usuario varchar(20),
estado_usuario varchar(20)
)

create table compras(
codigo_compra varchar(30) primary key,
codigo_usuario varchar(25),
sub_total float,
precio_total float,
fecha_compra date,
estado_compra varchar(20)
)

create table DetallesCompras(
codigo_compra varchar(30),
codigo_producto varchar(25),
nombre_producto varchar(100),
descripcion_producto varchar(100),
cantidad_compra int,
precio_producto float,
sub_total float,
descuento_compra float,
)
create table producto(
codigo_producto varchar(25) primary key,
nombre_producto varchar(100),
descripcion_producto varchar(100),
cantidad_producto int,
precioUnidad_producto float,
descuento_producto float,
imagen_producto varchar(100),
estado_producto varchar(15),
)